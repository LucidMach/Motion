{
  "name": "motion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --dir UI dev",
    "build": "pnpm --dir UI build",
    "build:db": "./build_gtfs_db.sh",
    "preview": "pnpm --dir UI preview"
  }
}
